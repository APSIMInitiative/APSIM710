#include <string>
#include <iostream>

// Extract file extension, port of internal Borland function
std::string ExtractFileExt(const char* a);

// Get current directory
std::string GetCurrentDirectory();

// show a message
void ShowMessage(std::string a);

// convert string to float
