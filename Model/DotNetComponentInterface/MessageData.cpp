#include "stdafx.h"
#include "MessageData.h"
/*
void pack(char* message, array<Double>^ Data)
	{
	pack(message, Data->Length);
	for (int i = 0; i != Data->Length; i++)
		pack(message, Data[i]);
	}
void unpack(char* message, array<Double>^% Data)
	{
	int count = 0;
	unpack(message, count);
	Data = gcnew array<double>(count);
	for (int i = 0; i != count; i++)
		{
		unpack(message, Data[i]);
		}
	}

unsigned memorySize(array<Double>^% Data)
	{
	return 4 + Data->Length * 8;
	}
*/	